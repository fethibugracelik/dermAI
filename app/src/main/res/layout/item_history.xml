<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="12dp"
    app:cardCornerRadius="16dp"
    app:cardElevation="6dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- Satır: Başlık + Sil Butonu -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <!-- Hastalık Adı -->
            <TextView
                android:id="@+id/textDisease"
                android:layout_width="0dp"
                android:layout_weight="1"
                android:layout_height="wrap_content"
                android:text="Vitiligo"
                android:textSize="18sp"
                android:textStyle="bold"
                android:textColor="#1A237E" />

            <!-- Sil Butonu -->
            <ImageButton
                android:id="@+id/deleteButton"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:padding="8dp"
                android:src="@android:drawable/ic_menu_delete"
                android:contentDescription="Sil"
                android:background="?attr/selectableItemBackgroundBorderless" />
        </LinearLayout>

        <!-- Güven Oranı -->
        <TextView
            android:id="@+id/textConfidence"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Güven: %87.2"
            android:textSize="14sp"
            android:textColor="#388E3C"
            android:layout_marginTop="4dp"/>

        <!-- Alternatif Tahminler -->
        <TextView
            android:id="@+id/textAlternatives"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Alternatif: Egzama (%8.1), Akne (%2.5)"
            android:textSize="13sp"
            android:textColor="#5D4037"
            android:layout_marginTop="4dp" />

        <!-- Tarih/Saat -->
        <TextView
            android:id="@+id/textTimestamp"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="2025-08-03 14:20"
            android:textSize="12sp"
            android:textColor="#9E9E9E"
            android:layout_marginTop="2dp"/>

    </LinearLayout>

</androidx.cardview.widget.CardView>
